# ~~G̶~~Jit

git compatible version control system in zig.

## Getting Started
\*required zig 0.16

Clone the repository

```bash 
git clone https://github.com/syeero7/jit
cd jit
```

Build the compiled binary

`zig build`

Run tests

`zig build test`
